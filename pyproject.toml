[tool.pytest.ini_options]
testpaths = ["tests"]
markers = ["integration: marks tests that require live Supabase connection"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I"]